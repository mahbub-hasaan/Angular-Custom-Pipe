import { SliceUpperPipe } from './slice-upper.pipe';

describe('SliceUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
